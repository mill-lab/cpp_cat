#include <iostream>
#include <cstring>
using namespace std;

int main()
{
  char name[64], sama[] = "様";

  cout << "名前を入力 --";
  cin.getline(name, 64);

  strcat(name, sama);
  cout << name << "ですか。待ってたよ" << endl;

  return 0;
}
