#include <iostream>
using namespace std;

int main()
{
  int a, b;
  a = 10;
  b = 4;
  
  cout << "a / b = " << a / b << endl;
  cout << "(double)a / b = " << (double)a / b << endl;
  cout << "double(a) / b = " << double(a) / b << endl;
  
  return 0;
}
